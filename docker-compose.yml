version: '3.8'

services:
  mongodb:
    image: docker.io/library/mongo:latest
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
      MONGO_INITDB_DATABASE: visiondb
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: